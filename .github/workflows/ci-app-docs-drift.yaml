name: CI App Docs Drift

# REQtec fork: upstream docs-drift gate. Manual only.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled on the REQtec internal fork"
