name: CD deploy tag

# REQtec fork: Twenty cloud tag deploy. Manual only.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled on the REQtec internal fork"
