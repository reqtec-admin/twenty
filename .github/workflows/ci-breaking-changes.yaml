name: GraphQL and OpenAPI Breaking Changes Detection

# REQtec fork: compares against Twenty's published schema. Manual only.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled on the REQtec internal fork"
