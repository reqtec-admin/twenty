name: Preview Environment Dispatch

# REQtec fork: dispatches twentyhq/ci-public with org secrets we do not have.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled on the REQtec internal fork"
