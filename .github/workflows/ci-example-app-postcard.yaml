name: CI Example App Postcard

# REQtec fork: example-app e2e needs a full spawned server. Manual only.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled on the REQtec internal fork"
