name: AI Catalog Sync

# REQtec fork: Twenty AI catalog sync. Manual only.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled on the REQtec internal fork"
